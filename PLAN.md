# PLAN.md

> PRD.md 기준 Steady Listening 전체 기능 구현을 위한 실행 계획.
> bkit `/pdca plan` 절차로 생성 (표준 위치 대신 사용자 요청에 따라 프로젝트 루트 단순 포맷 사용).

## 이번 사이클 목표

PRD.md에 정의된 must 기능 3가지(자동 학습자료 준비, 컨디션 기반 분량 조절, 복습용 화면)를
1주일 이내에 동작하는 MVP로 구현한다.

## 성공 기준

- 에피소드 준비(다운로드~전사~표현 추출)에 드는 수작업 시간을 0분으로 자동화한다.
- 컨디션이 낮은 날에도 학습 완료율을 주 5일 이상으로 유지한다 (분량 조절로 중단 방지).
- 복습용 화면에서 카테고리 필터를 선택하면 별도 로딩 없이 해당 표현 목록을 바로 확인할 수 있다.
- 반복 등장 표현을 기본형 기준으로 자동 집계해 복습 우선순위를 파악할 수 있다.

## 작업 목록 (먼저 되면 나머지가 쉬워지는 순서)

1. 프로젝트 뼈대 구성 (Next.js 프론트엔드 + FastAPI 백엔드 저장소 생성, 환경변수 설정 — `.env.example` + `.gitignore`로 비밀 정보 분리)
2. Supabase 프로젝트 생성 및 테이블 설계 (에피소드, 표현, 학습 세션)
3. RSS 파싱 및 최신 에피소드 GUID 확인 로직 구현
4. 처리된 GUID인지 판별해 신규 에피소드 여부를 가리는 로직 구현 (신규가 없으면 가장 최근 처리 에피소드를 홈에 계속 표시)
5. 오디오 다운로드 및 Supabase Storage 캐싱 구현 (재다운로드 방지)
6. faster-whisper(tiny 모델)로 오디오 전사 구현
7. OpenAI API(gpt-4o-mini)로 핵심 표현 최대 10개(중상급 이상 학습자 기준, 후보 부족 시 더 적어도 됨)·AI가 새로 생성한 예문 2개(+한국어 번역 포함)를 JSON 형식으로 생성하는 연동 구현 (2026-07-23: 예문 개수 3→2로 변경, 한국어 번역·선정 기준 추가는 이 시점부터 신규 처리분에만 적용)
8. 표현 저장 로직 구현 (실제 사용형+기본형 함께 저장, 기본형 기준 반복 횟수 계산)
9. 표현 카테고리 자동 분류 로직 구현 (여러 카테고리에 걸쳐도 가장 관련성 높은 하나만 선택, 해당 없으면 기타)
10. 에피소드·표현 데이터를 Supabase에 저장하는 연동 완성
11. RSS·전사·OpenAI API 실패 시 에러 배너+재시도 버튼 처리 구현 (RSS 실패 시 직전에 처리된 최신 에피소드는 그대로 유지) / 공식 처리가 아직 안 끝난 경우엔 재시도 버튼 없이 "아직 준비되지 않음" 상태만 표시하고 앱 실행마다 재확인
12. 오늘의 컨디션 선택 화면 구현 (높음/보통/낮음)
13. 오디오 플레이어 구현 (재생·일시정지·탐색바, 낮음 모드 100초 자동 정지)
14. 모드별 콘텐츠 노출 로직 구현 (표현 10개/5개/1개)
15. 완료 버튼 및 학습 세션 기록 구현 (날짜·모드 기록, 중복 클릭 방지)
16. 복습용 화면 구현 (카테고리 필터, 반복 표현 목록)
17. 모바일 반응형 스타일링 적용 (세이지 그린/톤다운 블루, 320~430px)
18. Vercel(프론트)·Render(백엔드) 배포 및 환경변수 설정
