# Steady Listening 서비스 기획서 (PRD)

- 작성자: 박지혜
- 작성일: 2026-07-22

## 1) 추진 배경 (문제 정의)
*누가, 어떤 업무에서, 어떤 불편을 겪는가*

매일 영어 리스닝을 습관으로 만들고 싶은 나 자신을 위한, 혼자 쓰는 개인용 앱이다. 컨디션이 나쁘거나 바쁜 날은 FT News Briefing 전체 분량을 듣는 것이 부담스러워 리스닝 루틴이 자꾸 끊긴다. 또한 에피소드에서 나온 표현을 정리하는 것 자체가 번거롭고, 정리해두어도 나중에 다시 꺼내보기 불편해서 학습이 쌓이지 않는다.

## 2) 현행 방식과 한계
*지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운가*

현재는 팟캐스트 앱에서 매번 직접 최신 에피소드를 확인해서 듣고, 인상 깊은 표현은 손으로 메모장이나 노트에 옮겨 적는 방식으로 진행하고 있다. 이 방식은 에피소드 확인부터 표현 정리까지 매번 수작업이 필요해 시간이 많이 들고, 정리한 표현이 한곳에 쌓이지 않아 나중에 검색하거나 복습하기 어렵다. 또한 컨디션과 무관하게 항상 전체 분량을 들어야 한다는 부담 때문에 며칠만 바빠도 루틴이 쉽게 끊긴다.

## 3) 목표와 기대효과 (성공 기준)
*측정 가능한 숫자로 (처리 시간, 문의 건수, 정답률 등)*

- 에피소드 준비(다운로드~전사~표현 추출)에 드는 수작업 시간을 0분으로 자동화한다.
- 컨디션이 낮은 날에도 학습 완료율을 주 5일 이상으로 유지한다 (분량 조절로 중단 방지).
- 복습용 화면에서 카테고리 필터를 선택하면 별도 로딩 없이 해당 표현 목록을 바로 확인할 수 있다.
- 반복 등장 표현을 기본형 기준으로 자동 집계해 복습 우선순위를 파악할 수 있게 한다.

## 4) 사용자와 이용 흐름
*누가 쓰는지와 사용 순서 (화살표로)*

사용자: 매일 영어 리스닝을 습관으로 만들고 싶은 나 자신 (1인 개인용 앱)

이용 흐름: 앱 실행 → RSS에서 새 에피소드 확인·자동 처리(스크립트/표현 준비) → 오늘의 컨디션 선택(높음/보통/낮음) → 컨디션에 맞는 분량 학습(듣기 + 표현 확인) → 완료 버튼 클릭 → 학습 완료 기록

## 5) 주요 기능 (must / nice로 구분)

**must**

1. 자동 학습자료 준비
   - 설명: 최신 FT News Briefing 에피소드를 자동으로 다운로드하고, whisper 전사문·핵심 표현·예문을 AI가 미리 만들어둔다.
   - AI가 꼭 지켜야 할 규칙:
     - 앱 실행 시 RSS(https://feeds.acast.com/public/shows/73fe3ede-5c5c-4850-96a8-30db8dbae8bf)에서 최신 에피소드를 확인할 것
     - RSS GUID를 기준으로 새 에피소드 여부를 판단할 것
     - 이미 처리된 에피소드는 다운로드·전사·API 분석을 반복하지 않을 것
     - 오디오 파일은 Supabase Storage에 캐싱해서 같은 에피소드를 재다운로드하지 않을 것 (백엔드 재배포·재시작에도 유지)
     - FT.com 공식 스크립트는 로그인(구독)이 있어야 열람 가능해 자동 수집이 어려우므로, faster-whisper로 오디오를 직접 전사해서 전사문으로 사용할 것
     - 전사는 OpenAI Whisper API(`whisper-1`)를 사용할 것 (2026-07-28 변경). 원래 faster-whisper로 Render 서버 안에서 직접 전사했으나, 무료 플랜(512MB) 메모리로는 전사 중 프로세스가 반복적으로 죽는 문제가 있어(2026-07-24~28), 전사를 API 호출로 옮겨 Render의 메모리 부담 자체를 없앴다
     - OpenAI API(gpt-4o-mini)로 핵심 표현을 최대 10개 추출하고(선정 기준에 맞는 후보가 부족하면 그보다 적어도 됨, 억지로 채우지 않음), 표현별로 AI가 새로 생성한 예문 2개를 만들 것 (2026-07-23부터 3개→2개로 변경, 기존 저장분은 그대로 둠)
     - 표현은 중상급 이상 학습자 기준으로 선정할 것 (초보자도 아는 기초 단어·표현 제외). 동사+명사구 콜로케이션, 관용적 set expression, 명사+명사 복합명사, 외신에 자주 등장하는 세련된 표현을 우선하고, 사람 이름·회사명·매체명·날짜 같은 고유명사는 제외할 것 (2026-07-23 추가)
     - 표현과 각 예문 모두 한국어 번역을 함께 생성해서 저장할 것
     - OpenAI API 응답은 일정한 JSON 형식으로 받을 것
     - 표현은 항상 원형(사전형)으로 정리할 것 — 전사문에 과거형·진행형 등으로 등장했더라도 그대로 저장하지 않는다. 예문 안에서는 자연스러운 시제를 자유롭게 써도 된다 (2026-07-23 변경, 기존 저장분은 그대로 둠)
     - 관련 표현/동의어를 1개 이상 함께 저장할 것 (2026-07-23 추가). 사용 맥락을 설명하는 문장(usage_note)은 한국어 뜻과 내용이 겹쳐 추가 가치가 없어 만들지 않는다 (2026-07-24 폐기)
     - 반복 등장 횟수는 원형(기본형) 기준으로 계산할 것
     - 복습 화면에서도 표현마다 예문과 한국어 번역을 함께 보여줄 것 (2026-07-23 추가)
     - 표현 저장은 로컬 SQLite가 아닌 Supabase(PostgreSQL)를 사용할 것
     - 표현을 경제 용어 카테고리(정책·재정, 통화·금리, 경제지표·시장, 무역·외교, 기업·AI, 인구·사회, 불확실성, 기타)로 자동 분류해서 저장할 것 (여러 카테고리에 걸치더라도 가장 관련성 높은 카테고리 하나만 선택할 것, 7개 어디에도 안 맞으면 "기타"로 분류할 것)
     - API 키는 코드에 직접 입력하지 않고 환경변수/설정파일로 분리할 것
     - API 호출 실패(타임아웃/레이트리밋) 시 앱이 멈추지 않고, 화면 상단 배너와 재시도 버튼으로 에러 상태를 보여줄 것
     - RSS 요청 실패 시에도 앱이 멈추지 않고, 화면 상단 배너와 재시도 버튼으로 에러 상태를 보여주며, 이전에 이미 처리된 최신 에피소드를 그대로 계속 보여줄 것
     - 공식 스크립트가 아직 게시되지 않았다면 재시도 로직 없이 앱 실행 시마다 다시 확인하고, "아직 준비되지 않음" 상태를 보여줄 것
     - Supabase Storage의 오디오 캐시는 별도 자동 삭제 없이 계속 보관하고, 필요 시 수동으로 삭제할 것
     - 그날 새 에피소드가 없으면 가장 최근에 이미 처리된 에피소드를 계속 보여줄 것

2. 컨디션 기반 분량 조절
   - 설명: 사용자가 오늘 에너지 수준을 선택하면, 그에 맞춰 학습 분량이 자동으로 달라진다.
   - AI가 꼭 지켜야 할 규칙:
     - 높음 = 전체 듣기 + 핵심 표현 10개 전체
     - 보통 = 전체 듣기 + 핵심 표현 5개
     - 낮음 = 제목 확인 + 첫 100초 듣기 + 오늘의 표현 1개
     - 세 모드 모두 완료 버튼을 누르면 학습 완료 날짜와 그날 선택한 모드(높음/보통/낮음)를 기록할 것
     - 같은 날 완료 버튼을 중복 클릭해도 세션이 중복 기록되지 않을 것
     - 사용자가 직접 입력(타이핑, 일지 작성)하는 동작은 없게 할 것
     - 오디오 플레이어는 재생·일시정지·탐색바만 기본으로 제공할 것
     - "낮음" 모드는 100초 지점에서 자동으로 재생을 정지할 것
     - 완료 버튼을 누르기 전까지는 모드를 자유롭게 바꿀 수 있으며, 실제로는 완료 버튼을 누른 시점의 모드만 기록할 것

3. 복습용 화면
   - 설명: 그동안 저장된 모든 표현을 카테고리별로 언제든지 다시 찾아볼 수 있는 화면. 기본형 기준으로 자동 집계된 반복 등장 횟수를 함께 보여줘 복습 우선순위를 파악할 수 있게 한다.

4. 완료 달력 (2026-07-23 추가)
   - 설명: 완료 버튼을 누른 날짜를 달력에 표시해서, 매일 하는 것이 목표인 습관을 한눈에 확인할 수 있게 한다.
   - AI가 꼭 지켜야 할 규칙:
     - 완료 기록은 세션이 실제로 기록된 날짜(=그날 학습한 에피소드가 처리된 날짜, KST 기준) 칸에 체크 표시할 것
     - 이번 달 평일 완료 일수 / 전체 평일 수를 함께 보여줄 것 (주말·미래 날짜는 분모에서 제외)
     - 스트릭(연속 일수)을 강조하는 게이미피케이션 UI는 만들지 않을 것 (6절 비범위 참고)

**nice**

- 이번 범위에는 없음 (핵심 기능 2가지에 집중)

## 6) 범위와 비범위
*이번 실습에서 만들 것 / 만들지 않을 것*

**범위 (만들 것)**
- 자동 학습자료 준비 (RSS 확인, 스크립트 확보, 표현/예문 생성 및 저장)
- 컨디션 기반 분량 조절 (높음/보통/낮음 모드 및 학습 완료 기록)
- 복습용 화면 (반복 등장 표현 확인)
- 완료 달력 (매일 완료 여부를 달력으로 확인, 2026-07-23 추가)

**비범위 (만들지 않을 것)**
- 지난 에피소드 목록/다시듣기 기능 (홈 화면은 항상 오늘 처리된 에피소드 하나만 보여줌 — "오늘 할 수 있는 만큼만" 하루 단위 습관 앱 컨셉 유지. 2026-07-23 확인. 단, 지난 오디오·표현 데이터는 삭제하지 않고 계속 보관하며 복습 화면에서는 계속 조회 가능)
- 받아쓰기(딕테이션) 기능
- 스피킹/발음 평가 기능
- 다른 팟캐스트·다른 언어 소스 확장
- 연속 학습일수를 크게 강조하는 "스트릭" 형태의 게이미피케이션 UI
- 오프라인 사용 지원 (서버와 통신이 필요한 온라인 웹앱으로만 동작)
- 홈 화면 추가(PWA) 지원 — 브라우저 즐겨찾기로 충분
- 새 에피소드 알림(이메일·푸시 알림) — 이번 범위에서는 만들지 않음 (앱이 무거워지는 것을 피하기 위해)

## 7) 보안·개인정보 검토
*사용 문서의 공개등급, 개인정보 여부, API 키 관리 방법*

- 공개등급: FT News Briefing은 공개 팟캐스트 콘텐츠이며, 처리 결과(스크립트·표현)는 개인 학습 용도로만 사용하고 외부에 공개하지 않는다.
- 개인정보 여부: 혼자 쓰는 개인용 앱으로, 회원가입이나 타인의 개인정보를 수집하지 않는다. 저장되는 데이터는 학습 세션 기록, 표현 데이터, 에피소드 오디오 캐시, faster-whisper로 생성한 전사문이다.
- API 키 관리: OpenAI API 키와 Supabase 접속 정보는 코드에 직접 입력하지 않고 환경변수(.env) 또는 별도 설정파일로 분리하여 관리하고, 저장소에 커밋되지 않도록 한다.
- 접속 방법·접근 제한: 별도 회원 인증 없이 배포된 URL로 접속한다. 개인용 앱이므로 URL 자체를 비공개로 유지하는 선에서 접근을 제한한다.

## 8) 기술 스택
*개발에 사용할 도구 모음 (난이도별 옵션 중 내가 고른 것과 그 이유)*

**선택:** Next.js(React) 프론트엔드 + FastAPI(Python) 백엔드 + faster-whisper(오디오 전사, tiny 모델) + Supabase(PostgreSQL) + Vercel(프론트)/Render 무료 플랜(백엔드) 배포

**배포 참고:** Render 무료 플랜은 일정 시간 요청이 없으면 서버가 잠들었다가 다시 요청 시 깨어나며, 이 경우 첫 로딩이 지연될 수 있다. 개인용 습관 앱 특성상 이 지연은 감수하고 무료 플랜을 사용한다.

**배포 현황 (2026-07-24):** 프론트엔드 https://steady-listening.vercel.app , 백엔드 https://steady-listening-backend.onrender.com 배포 완료. GitHub 저장소는 비공개([sghjjh66-cmyk/steady-listening](https://github.com/sghjjh66-cmyk/steady-listening)). 배포 후 실제로 겪은 문제와 대응은 `CHECK.md` 5절 참고 (RSS 요청 타임아웃 부재, 무료 플랜 메모리 부족으로 인한 전사 중 프로세스 종료, 홈 화면이 RSS 확인을 기다리지 않도록 백그라운드 처리로 전환 등).

**이유:**
- PRD의 "앱 실행 시 RSS 확인" 규칙은 앱을 열 때 백엔드가 확인하는 방식으로 충분해서, 별도의 자동 스케줄러(크론) 없이도 구현할 수 있다.
- 프론트(Next.js)와 백엔드(FastAPI)가 분리되어 있어 각각 익숙한 방식으로 만들 수 있고, 세이지 그린 톤의 차분한 모바일 화면을 자유롭게 구현할 수 있다.
- 서버리스 함수·크론 같은 새 개념을 배울 필요가 없어, 매일 쓰는 개인용 앱을 실제로 오래 유지보수하기에 적당한 복잡도다.

## 9) 개발 단위

**목표 기한: 1주일 이내 완성**

1. 프로젝트 뼈대 구성 (Next.js 프론트엔드 + FastAPI 백엔드 저장소 생성, 환경변수 설정)
2. Supabase 프로젝트 생성 및 테이블 설계 (에피소드, 표현, 학습 세션)
3. RSS 파싱 및 최신 에피소드 GUID 확인 로직 구현
4. 신규 에피소드 판별 로직 구현 (처리된 GUID면 건너뛰기)
5. 오디오 다운로드 및 Supabase Storage 캐싱 구현
6. 오디오 전사 구현 (전사문으로 사용) — 최초 faster-whisper(tiny.en)로 구현했으나 2026-07-28 OpenAI Whisper API로 교체
7. OpenAI API(gpt-4o-mini) 연동 - 핵심 표현 최대 10개·예문 2개 생성 (JSON 응답 파싱)
8. 표현 저장 로직 구현 (실제 사용형·기본형 함께 저장, 반복 횟수 계산)
9. 표현 카테고리 자동 분류 로직 구현
10. Supabase에 에피소드·표현 데이터 저장 연동
11. RSS·OpenAI API 실패 시 에러 상태 처리 구현
12. 오늘의 컨디션 선택 화면 구현 (높음/보통/낮음)
13. 오디오 플레이어 구현 (재생·일시정지·탐색바, 낮음 모드 100초 자동 정지)
14. 모드별 콘텐츠 노출 로직 구현 (표현 10개/5개/1개)
15. 완료 버튼 및 학습 세션 기록 구현 (날짜·모드 기록, 중복 방지)
16. 복습용 화면 구현 (카테고리 필터, 반복 표현 목록)
17. 모바일 반응형 스타일링 적용 (세이지 그린/톤다운 블루, 320~430px)
18. 완료 달력 화면 구현 (평일 완료 일수 집계, 주말·미래 날짜 제외)
19. Vercel(프론트)·Render(백엔드) 배포 및 환경변수 설정
20. 배포 후 발견된 문제 대응: RSS 요청 타임아웃 추가, 전사 파이프라인 메모리 최적화(중복 다운로드 제거·tiny.en 모델) 시도 후에도 Render 메모리 부족이 반복돼 전사를 OpenAI Whisper API 호출로 교체(2026-07-28), 홈 화면 응답을 캐시 즉시 반환+백그라운드 확인 구조로 전환
