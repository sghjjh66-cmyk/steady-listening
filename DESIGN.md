# DESIGN.md

> PLAN.md를 바탕으로 한 설계 문서. bkit `/pdca design` 절차로 생성 (프로젝트 루트 단순 포맷).

## 1. 화면 구성

### 화면 1 — 홈 / 컨디션 선택
- 상단: 오늘 처리된 최신 에피소드 제목·날짜
- 중앙: 컨디션 선택 버튼 3개 (높음 / 보통 / 낮음)
- 상단 배너 자리 (평소엔 숨김, 아래 두 상태를 구분해서 노출):
  - **실패 상태**: RSS·전사·OpenAI API 실패 시 에러 메시지 + 재시도 버튼
  - **준비 안됨 상태**: 공식 처리가 아직 끝나지 않은 경우 "오늘 에피소드 아직 준비되지 않음" 메시지만 표시, **재시도 버튼 없음** (앱을 다시 열 때마다 자동으로 재확인)

### 화면 2 — 학습 화면 (컨디션 선택 후 진입)
- 상단: 에피소드 제목 (낮음 모드의 "제목 확인" 요구사항 포함 — 모든 모드에서 공통 노출)
- 오디오 플레이어: 재생/일시정지 버튼 + 탐색바만 (녹음·다운로드 등 부가 기능 없음, 타이핑·일지작성 등 사용자 직접 입력 UI 없음)
- 오디오 플레이어 바로 아래: **접기/펼치기 버튼("스크립트 보기")** — 펼치면 전사 스크립트 전문이 스크롤 가능한 박스로 표시됨 (평소엔 접힌 상태, 2026-07-23 추가)
- 표현 카드 목록: 선택한 모드에 따라 1개(낮음) / 최대 5개(보통) / 전체(높음, 최대 10개)만 노출
  - 카드 레이아웃 (2026-07-24 최종): 좌측에 표현(원형, 그린 볼드 헤드워드) / 우측 상단에 카테고리(연한 회색)·한국어 뜻 순으로 배치. 그 아래 "Related expressions: ..." 관련 표현 목록. 예문은 회색 박스 하나에 영문+한국어 번역을 함께 담고, 문장 안에서 실제 활용된 표현 부분(highlight)만 그린 볼드로 강조 표시 (예: "drop unexpectedly" 원형이 예문에서 "dropped unexpectedly"로 강조됨). 2026-07-23: 예문 3개→2개, 파스텔 2색 박스 대신 단일 회색 박스+인라인 강조 방식으로 변경. 2026-07-24: 사용 맥락 설명(usage_note)은 한국어 뜻과 내용이 겹쳐 추가 가치가 없어 제거 (프롬프트에서도 생성 안 함)
  - 재생 중인 오디오 위치에 비례해서, 지금 구간에 해당할 것으로 보이는 카드를 테두리+🔊로 하이라이트 (전사 등장 순서 기반 근사치, 2026-07-24 추가)
- 스크립트 보기를 펼치면, 전사문 안에서 표현이 원형 그대로 등장하는 부분을 그린 볼드로 강조 (활용형으로 등장해 못 잡아내는 경우는 오탐 방지를 위해 그냥 둠, 2026-07-24 추가)
- 오디오 플레이어는 `controlsList="nodownload"`로 다운로드 버튼을 숨김 (2026-07-24 추가, "부가 기능 없음" 요구사항 반영)
- 완료 버튼은 화면 하단에 고정(`fixed`)되어 카드 개수와 무관하게 항상 보임 (2026-07-24 수정, 이전엔 일반 흐름에 있어 높음 모드에서 화면 아래로 밀려나는 문제가 있었음)
  - 낮음 모드의 "오늘의 표현 1개"는 그날 표현 목록 중 **반복 등장 횟수가 가장 높은 표현**을 선택 (동률이면 목록 순서상 첫 번째)
- 하단 고정: 완료 버튼 (누르면 학습 세션 기록 후 홈으로 복귀)

### 화면 3 — 복습 화면
- 상단: 카테고리 필터 칩 (정책·재정 / 통화·금리 / 경제지표·시장 / 무역·외교 / 기업·AI / 인구·사회 / 불확실성 / 기타)
- 목록: 필터 선택 시 별도 로딩 없이 즉시 갱신되는 표현 목록. 학습 화면과 동일한 카드(표현+한국어 번역+관련 표현+예문 2개)를 보여주고, 반복 등장 횟수 배지를 추가로 표시 (2026-07-23: 예문·번역 표시 추가)

## 2. 데이터 흐름 (입력 → 처리 → 출력)

**A. 에피소드 준비 흐름**

`/episodes/status` 호출 시 **DB에 저장된 최신 에피소드를 즉시 반환**하고, 아래 신규 에피소드 확인·처리는
응답을 보낸 뒤 백그라운드로 실행한다 (2026-07-24 변경 — RSS 조회가 15~20초 걸리고 Render 무료 인스턴스
콜드스타트까지 겹치면 홈 화면이 멈춘 것처럼 보이는 문제 대응). 저장된 에피소드가 하나도 없는 최초 실행일 때만
예외적으로 아래 처리를 동기로 기다린다.

```
RSS 피드 확인 (GUID 비교, requests+timeout으로 응답 없으면 20초 후 에러 처리)
   → [신규 에피소드 없음] 가장 최근 처리본 유지, 종료
   → [공식 처리가 아직 게시되지 않음] 재시도 없이 "준비되지 않음" 상태 표시, 다음 실행 시 재확인
   → [신규 에피소드 있음]
       → 오디오 다운로드 → Supabase Storage에 캐싱 (자동 삭제 없음, 필요 시 수동 삭제만) — 다운로드한 바이트는 전사 단계에서 재사용해 같은 오디오를 두 번 받지 않는다
       → OpenAI Whisper API(`whisper-1`)로 전사 → 전사문 텍스트 (episodes 테이블에 함께 저장). 원래 faster-whisper로 Render 서버 안에서 직접 전사했으나 무료 플랜 메모리 부족으로 반복 실패해 API 호출로 변경 (2026-07-28)
       → OpenAI API(gpt-4o-mini)에 전사문 전달
           → 중상급 이상 학습자 기준 핵심 표현 최대 10개(원형, 후보 부족 시 더 적어도 됨) + 관련 표현 + 한국어 번역 + AI가 새로 생성한 예문 2개를 JSON으로 응답 (usage_note는 2026-07-24 폐기)
       → 표현마다 카테고리 자동 분류(가장 관련성 높은 하나만) + 반복 횟수 집계 (원형 기준)
       → Supabase(episodes, expressions 테이블)에 저장
```
(RSS·전사·OpenAI API 실패 시: 최초 실행 동기 처리 경로에서는 에러 배너+재시도로 화면에 전달하고, 그중 RSS 실패는 이전 최신 에피소드를 그대로 유지. 백그라운드 처리 중 실패하면 조용히 넘어가고 다음 접속 때 다시 시도한다. "준비되지 않음" 상태는 실패가 아니라 별도 상태이므로 재시도 버튼을 두지 않는다.)

**B. 학습 흐름 (사용자 조작)**

```
사용자가 컨디션(높음/보통/낮음) 선택
   → 프론트가 모드에 맞는 분량만 화면에 표시 (서버에서 전체 데이터는 이미 받아둔 상태)
   → 오디오 재생 (낮음 모드는 100초에서 자동 정지)
   → 완료 버튼 클릭
       → 학습 세션 기록 API 호출 (날짜 + 선택한 모드)
       → Supabase(sessions 테이블)에 저장. **같은 날 이미 완료 기록이 있으면 새로 기록하지 않고 최초 완료 시점의 모드를 그대로 유지** (나중에 다른 모드로 다시 완료를 눌러도 기존 기록을 덮어쓰지 않음 — "완료 버튼을 누른 시점의 모드만 기록"을 최초 1회 기준으로 해석)
```

**C. 복습 흐름**

```
사용자가 카테고리 필터 선택
   → 이미 불러온 전체 표현 목록에서 프론트가 즉시 필터링 (서버 재요청 없음 → "별도 로딩 없이" 요구사항 충족)
   → 기본형 기준 반복 횟수 순으로 정렬해 표시
```

## 3. 보안 / 개인정보

- OpenAI API 키, Supabase 접속 정보는 코드에 직접 넣지 않고 환경변수(.env)로 분리하며, `.gitignore`에 등록해 저장소에 커밋되지 않게 한다. 저장소에는 `.env.example`(키 이름만, 값은 비움)만 커밋한다.
- 회원가입/로그인 없이 배포된 URL로 접속하는 개인용 앱이므로, 접근 제한은 URL 자체를 비공개로 유지하는 방식을 기본으로 한다.
- **공유 비밀 헤더(2026-07-23 추가, 배포 전 보안 점검 결과 반영)**: `/health`·문서 경로를 제외한 모든 API가 `X-App-Key` 헤더(`APP_SHARED_SECRET`과 일치)를 요구한다. 크롤러·링크 미리보기봇 등이 무심코 두드려서 OpenAI·whisper 비용이 새는 것을 막기 위함이며, 결연한 공격자를 막는 용도는 아니다(프론트 번들에 키가 그대로 노출되므로). CORS는 `ALLOWED_ORIGINS`에 등록된 배포 프론트엔드 주소만 허용한다.
- 저장되는 데이터: 학습 세션 기록, 표현 데이터, 에피소드 오디오 캐시, faster-whisper로 생성한 전사문 — 전부 개인 학습 용도로만 사용하고 외부에 공개하지 않는다.

## 4. 기술 선택 (난이도별 3가지 옵션)

비개발자 기준으로 쉬운 설명 1줄씩 정리했습니다. **추천: 옵션 B (PRD에서 이미 선택한 조합)**

| 옵션 | 구성 | 쉬운 설명 (1줄) | 난이도 |
|------|------|----------------|:------:|
| **A. 올인원** | Next.js + Vercel 서버 기능(API Routes)만 사용, 별도 백엔드 없음 | 화면 만드는 도구 하나로 서버 역할까지 같이 처리하는 방법 — 배우고 관리할 게 가장 적어요 | 쉬움 |
| **B. 분리형 (PRD 선택)** | Next.js(화면) + FastAPI(파이썬 서버) + Supabase(저장소) | 화면 담당과 뒤에서 무거운 작업(전사·AI 호출)을 처리하는 서버 담당을 따로 두는 방법 — 이번 PRD에서 고른 방식이에요 | 중간 |
| **C. 작업 줄 세우기형** | Next.js + FastAPI + 작업 큐(Celery/Redis 등) + Supabase | 전사처럼 시간이 오래 걸리는 작업을 별도 줄에 세워 처리해서, 그 사이에도 앱이 멈추지 않게 하는 방법 — 배울 게 많지만 나중에 사용자가 늘어도 안정적이에요 | 어려움 |

**추천 이유**: PRD 8절에서 이미 "별도 스케줄러 없이 앱 실행 시 확인"으로 충분하다고 정의했고, 1인용 개인 앱이라 트래픽이 크지 않습니다. 옵션 C의 작업 큐는 지금 단계에서는 과한 복잡도이고, 옵션 A는 무거운 전사 작업(faster-whisper)을 Vercel 서버리스 함수에서 돌리기엔 실행 시간 제한이 걸릴 수 있어 적합하지 않습니다. 옵션 B가 PRD의 선택과도 일치하고 지금 규모에 가장 적당합니다.
